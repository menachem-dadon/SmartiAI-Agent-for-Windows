# Source-development compatibility entrypoint while the legacy PyQt client is
# retained through migration Point 16. Production Tauri builds use
# requirements-core.txt and contain no Qt packages.
-r requirements-legacy-ui.txt
